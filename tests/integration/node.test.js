describe('Node - Integration', () => {
  test('generate mjml email from config', () => {

  });

  test('generate html email from config', () => {

  });

  test('save emails from config', () => {

  });
});